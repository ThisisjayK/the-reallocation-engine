## 2026-07-17 -- case-infosys-healthtech-pm-opt: first sample run (Mode Build Assignment)

- **Recipe:** case-infosys-healthtech-pm-opt v0.1.0 (new)
- **Inputs:** `data/examples/case-healthtech-pm-opt-roles.json`, `case-healthtech-pm-opt-profile.json` (new fixtures, no personal data -- illustrative authorization string only); read (not modified) `data/80-days-to-stay/data/SEC_DOL_H1b_data_mapped.csv` and `data/bls/compact/soc_occupation_compact.csv`.
- **Commands:** `npm run verify` -- `python3 scripts/ats/detect-ats.py "Omada Health" "Oula Health" "Modern Health" "Castle Biosciences, Inc."` -- `npm run ats:scan -- --dry-run` (and once live, to populate `data/ats/pipeline.md`) -- `npm run ats:liveness -- <3 urls>` -- `node scripts/score/role-scorer.mjs data/examples/case-healthtech-pm-opt-roles.json --profile data/examples/case-healthtech-pm-opt-profile.json` -- `npm run doctor`.
- **Outputs:** `recipes/case-infosys-healthtech-pm-opt.md`, `data/examples/case-healthtech-pm-opt-{roles,profile}.json`, `data/examples/case-healthtech-pm-opt-scores.md`, `assignments/submissions/kappagantula/{domain-justification,worked-run}.md`.
- **Result:** Apply 0 / Consider 2 / Skip 1 (skip 33%) on 3 real/realistic roles. Live ATS scan found 54 real postings across Omada/Oula/Modern Health, 9 matching this mode's title filter; liveness gate correctly zeroed the highest-vote-sum role (0.435) because its posting -- verified live 2026-06-21 in a prior exercise -- had expired by 2026-07-17. Found and worked around a real cross-script bug: `providers/greenhouse.mjs` only resolves `job-boards.greenhouse.io` URLs even though `boards.greenhouse.io` is in its own trusted-host allowlist and is what `detect-ats.py` itself outputs.
- **Open issues:** `role-scorer.mjs`'s sponsorship model has no field for the E-Verify-now/H-1B-later two-tier constraint this population actually has (`profile_needs_sponsorship: true` mis-frames it as a single immediate gate) -- typed `[TODO: DEV]` in the recipe. `role_quality` weight is `0.0` by default (repo-wide, pre-existing `[VERIFY]`) so the Cognitive Pivot signal is computed but not yet load-bearing in this mode's composite. Castle Biosciences (real H-1B history for an `IT, Product Owner` title) could not be liveness-checked -- its ATS slug didn't auto-detect. Greenhouse URL-format bug not fixed upstream, only worked around locally.
- **Doctor reconciliation:** first `npm run doctor` pass after writing the recipe flagged `todos_open` mismatch (frontmatter declared 4, body markers counted 8) -- 4 were genuine typed TODOs, 4 were prose cross-references reusing the `[TODO: TYPE]` bracket syntax to point back at them. De-bracketed the 4 references (kept the meaning, dropped the marker syntax) rather than inflate the declared count for references that aren't separate open items. Re-ran `npm run doctor`: 521 declared == 521 body markers repo-wide, clean.
